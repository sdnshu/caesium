{
  "name": "caesium",
  "scripts": {
    "dev": "bunx drizzle-kit migrate && bun run --hot ./index.ts",
    "db:generate": "bunx drizzle-kit generate",
    "db:migrate": "bunx drizzle-kit migrate",
    "db:push": "bunx drizzle-kit generate && bunx drizzle-kit migrate",
    "db:studio": "bunx drizzle-kit studio --verbose"
  },
  "dependencies": {
    "better-auth": "^1.3.28",
    "drizzle-orm": "^0.44.6",
    "hono": "^4.9.10",
    "pg": "^8.16.3",
    "postgres": "^3.4.7"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/pg": "^8.15.5",
    "drizzle-kit": "^0.31.5"
  }
}